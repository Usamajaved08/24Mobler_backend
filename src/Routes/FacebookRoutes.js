import express from "express";
// import { facebookLogin } from "../Controllers/UserController.js";
const router = express.Router();

// router.post("/facebook", facebookLogin);

export default router;
